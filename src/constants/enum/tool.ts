/**
 * 工具类型枚举
 */
export enum ToolEnum {
  SELECT = 'select',
  HAND = 'hand',
  RECT = 'rect',
  CIRCLE = 'circle',
  LINE = 'line',
  PATH = 'path',
  TEXT = 'text',
  CROP = 'crop',
  ERASER = 'eraser',
  BRUSH = 'brush',
  FILL = 'fill',
  TRANSFORM = 'transform',
}
